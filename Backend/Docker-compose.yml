version: "3.9"
services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "5000:5000"
    container_name: backend
    restart: always
    command: python manage.py runserver 0.0.0.0:5000


